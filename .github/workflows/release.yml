# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [GitHubActions (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration GitHubActions_release --host GitHubActions
#
# </auto-generated>
# ------------------------------------------------------------------------------

name: release

on:
  push:
    tags:
      - 'v*.*'

jobs:
  windows-latest:
    name: windows-latest
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - name: 'Cache: .nuke/temp, ~/.nuget/packages'
        uses: actions/cache@v3
        with:
          path: |
            .nuke/temp
            ~/.nuget/packages
          key: ${{ runner.os }}-${{ hashFiles('**/global.json', '**/*.csproj', '**/Directory.Packages.props') }}
      - name: 'Run: Publish'
        run: ./build.cmd Publish
      - name: 'Publish: tasktitan-win-Setup.exe'
        uses: actions/upload-artifact@v3
        with:
          name: tasktitan-win-Setup.exe
          path: release/win-x64/tasktitan-win-Setup.exe
      - name: 'Publish: tasktitan-win-Portable.zip'
        uses: actions/upload-artifact@v3
        with:
          name: tasktitan-win-Portable.zip
          path: release/win-x64/tasktitan-win-Portable.zip
